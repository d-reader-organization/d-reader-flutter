enum FilterId {
  free,
  popular,
}

enum SortByEnum {
  latest,
  rating,
  likes,
  readers,
  viewers,
  followers,
  name,
  published,
}

enum SortDirection { asc, desc }

enum ViewMode {
  gallery,
  detailed,
}
