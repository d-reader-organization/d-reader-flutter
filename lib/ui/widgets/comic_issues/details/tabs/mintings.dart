import 'package:flutter/material.dart';

class IssueMintings extends StatelessWidget {
  const IssueMintings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
